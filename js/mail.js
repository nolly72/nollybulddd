document.addEventListener('DOMContentLoaded', () => {
    // Инициализация библиотеки AOS
    AOS.init({
        duration: 1200, // Скорость анимации
        once: false,    // Срабатывает при каждом скролле (вверх и вниз)
    });
});
